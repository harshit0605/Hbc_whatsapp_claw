services:
  neo4j:
    image: neo4j:5
    container_name: talkgraph-neo4j
    ports:
      - "7474:7474"   # HTTP browser
      - "7687:7687"   # Bolt
    environment:
      NEO4J_AUTH: neo4j/talkgraph-dev
      NEO4J_PLUGINS: '[]'
    volumes:
      - neo4j-data:/data

volumes:
  neo4j-data:
